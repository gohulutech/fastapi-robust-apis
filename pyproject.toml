[project]
name = "fast-api-robust-apis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hugo Arevalo", email = "gohulu@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "annotated-types>=0.8.0",
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.52",
    "tabulate>=0.10.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
fast-api-robust-apis = "fast_api_robust_apis:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
